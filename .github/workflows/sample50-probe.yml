name: Sample50 corporate-action probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe retired
        run: echo 'Probe completed; see prior artifacts. Use Sample50 stage workflows for further validation.'
